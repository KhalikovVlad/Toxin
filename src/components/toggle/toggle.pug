mixin toggle(toggle)
    -
        const {
            name,
            label,
            checked=false,
            disabled=false
        } = toggle;
        
    label.toggle
        input.toggle__input(name = name, type = 'checkbox', checked = checked ? true : false, disabled = disabled ? true : false)
        
        if label
            span.toggle__label #{label}